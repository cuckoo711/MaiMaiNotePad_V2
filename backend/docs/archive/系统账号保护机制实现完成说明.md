# 系统账号保护机制实现完成说明

## 概述

本文档记录了 `ai_reviewer` 系统账号保护机制的完整实现过程和验证结果。

## 实现时间

2026-03-07

## 背景

`ai_reviewer` 是 AI 自动审核系统使用的系统账号，用于记录 AI 审核操作。该账号需要特殊保护，确保：

1. 在系统初始化时自动创建
2. 标记为系统账号，无法被删除
3. 禁止登录
4. 不能被处罚（禁言/封禁）

## 实现内容

### 1. 数据模型层

#### 1.1 Users 模型扩展

**文件**: `backend/mainotebook/system/models.py`

**修改内容**:

1. 在 `USER_TYPE` 选项中添加系统账号类型：
   ```python
   USER_TYPE = (
       (0, "后台用户"),
       (1, "前台用户"),
       (2, "系统账号"),  # 新增
   )
   ```

2. 添加三个保护方法：

   ```python
   def is_system_account(self) -> bool:
       """判断是否为系统账号
       
       Returns:
           bool: 如果是系统账号返回 True，否则返回 False
       """
       return self.user_type == 2

   def can_be_moderated(self) -> bool:
       """判断是否可以被处罚（禁言/封禁）
       
       系统账号不能被处罚
       
       Returns:
           bool: 如果可以被处罚返回 True，否则返回 False
       """
       return not self.is_system_account()

   def can_be_deleted(self) -> bool:
       """判断是否可以被删除
       
       系统账号不能被删除
       
       Returns:
           bool: 如果可以被删除返回 True，否则返回 False
       """
       return not self.is_system_account()
   ```

#### 1.2 数据库迁移

**文件**: `backend/mainotebook/system/migrations/0006_add_system_account_type.py`

**内容**: 添加系统账号类型到 `user_type` 字段的选项中

**执行命令**:
```bash
conda activate mai_notebook
python manage.py migrate system
```

**执行结果**: ✓ 迁移成功应用

### 2. 初始化配置

#### 2.1 用户初始化配置

**文件**: `backend/mainotebook/system/fixtures/init_users.json`

**添加内容**:
```json
{
    "username": "ai_reviewer",
    "email": "ai_reviewer@system.mainotebook.com",
    "mobile": "",
    "avatar": null,
    "name": "AI 审核员",
    "gender": 0,
    "user_type": 2,
    "role": [],
    "role_key": [],
    "dept_key": "mainotebook",
    "first_name": "",
    "last_name": "",
    "is_staff": false,
    "is_active": false,
    "password": "!",
    "last_login": null,
    "is_superuser": false
}
```

**关键配置说明**:
- `user_type: 2` - 标记为系统账号
- `is_active: false` - 禁用账号，无法登录
- `password: "!"` - 无效密码，确保无法通过密码验证登录
- `role_key: []` - 无角色，无任何权限

### 3. 业务逻辑层

#### 3.1 用户处罚服务保护

**文件**: `backend/mainotebook/content/services/user_moderation_service.py`

**修改方法**: `validate_permission()`

**添加内容**:
```python
# 规则0：不能操作系统账号
if target_user.is_system_account():
    logger.warning(
        "权限验证失败：用户 %s 尝试对系统账号 %s 执行操作",
        operator.username, target_user.username
    )
    return False
```

**影响范围**: 所有处罚操作（禁言、封禁、解除、修改时长）都会先调用此方法验证权限

### 4. API 层

#### 4.1 用户删除保护

**文件**: `backend/mainotebook/system/views/user.py`

**添加方法**: `UserViewSet.destroy()`

**实现内容**:
```python
def destroy(self, request, *args, **kwargs):
    """删除用户
    
    系统账号不能被删除。
    
    Returns:
        Response: 删除结果
    """
    instance = self.get_object()
    
    # 检查是否为系统账号
    if not instance.can_be_deleted():
        return ErrorResponse(msg="系统账号不能被删除")
    
    # 执行删除
    self.perform_destroy(instance)
    return DetailResponse(data=None, msg="删除成功")
```

### 5. 前端层

#### 5.1 处罚操作页面保护

**文件**: `admin_web/src/views/content/moderation/punish/index.vue`

**修改内容**:

1. 在操作列模板中添加系统账号判断：
   ```vue
   <template #row-handle="{ row }">
       <!-- 系统账号不显示任何操作按钮 -->
       <template v-if="row.user_type !== 2">
           <el-button>禁言</el-button>
           <el-button>封禁</el-button>
           <el-button>查看详情</el-button>
       </template>
       <template v-else>
           <el-tag type="info" size="small">系统账号</el-tag>
       </template>
   </template>
   ```

**效果**: 系统账号在处罚操作页面只显示"系统账号"标签，不显示任何操作按钮

## 数据修复

### 修复现有 ai_reviewer 账号

由于 ai_reviewer 账号可能在实现保护机制前已经创建，需要更新其属性。

**修复脚本**: `backend/fix_ai_reviewer_account.py`

**执行命令**:
```bash
conda activate mai_notebook
python fix_ai_reviewer_account.py
```

**修复内容**:
- `user_type`: 0 → 2
- `is_active`: True → False
- `password`: (原密码) → "!"

**执行结果**: ✓ 修复成功

## 测试验证

### 测试脚本

**文件**: `backend/test_system_account_protection.py`

**测试项目**:

1. ✓ 查找 ai_reviewer 账号
2. ✓ 验证 user_type=2
3. ✓ 验证 is_system_account() 方法
4. ✓ 验证 can_be_moderated() 方法
5. ✓ 验证 can_be_deleted() 方法
6. ✓ 验证无法登录（is_active=False, password="!"）
7. ✓ 对比普通用户的方法返回值

**执行命令**:
```bash
conda activate mai_notebook
python test_system_account_protection.py
```

**测试结果**: ✓ 所有测试通过

### 测试输出

```
============================================================
系统账号保护机制测试
============================================================

[测试 1] 查找 ai_reviewer 账号...
✓ 找到账号: ai_reviewer
  - ID: 67b0d447-d8c8-4187-86f1-73e4e076bdf4
  - 名称: AI 审核员
  - 用户类型: 2 (系统账号)
  - 是否激活: False
  - 密码: !...

[测试 2] 验证 user_type=2...
✓ user_type 正确设置为 2（系统账号）

[测试 3] 验证 is_system_account() 方法...
✓ is_system_account() 返回 True

[测试 4] 验证 can_be_moderated() 方法...
✓ can_be_moderated() 返回 False（系统账号不能被处罚）

[测试 5] 验证 can_be_deleted() 方法...
✓ can_be_deleted() 返回 False（系统账号不能被删除）

[测试 6] 验证无法登录...
✓ is_active=False（账号已禁用）
✓ password='!'（无效密码，无法登录）

[测试 7] 对比普通用户...
  普通用户: admin_test
  - is_system_account(): False
  - can_be_moderated(): True
  - can_be_deleted(): True
✓ 普通用户的方法返回值正确

============================================================
✓ 所有测试通过！系统账号保护机制正常工作
============================================================
```

## 保护机制总结

### 四层保护

1. **数据模型层**: 通过 `user_type=2` 标识系统账号，提供判断方法
2. **业务逻辑层**: 在处罚服务中验证权限，拒绝对系统账号的操作
3. **API 层**: 在删除接口中检查，拒绝删除系统账号
4. **前端层**: 在 UI 中隐藏操作按钮，显示"系统账号"标签

### 登录保护

- `is_active=False`: 账号被禁用
- `password="!"`: 无效密码，无法通过任何密码验证

### 权限保护

- `role_key=[]`: 无任何角色
- `is_staff=false`: 非管理员
- `is_superuser=false`: 非超级管理员

## 影响范围

### 后端

- ✓ 数据模型: Users 模型
- ✓ 数据库迁移: 0006_add_system_account_type
- ✓ 初始化配置: init_users.json
- ✓ 业务逻辑: user_moderation_service.py
- ✓ API 接口: UserViewSet.destroy()

### 前端

- ✓ 处罚操作页面: punish/index.vue

### 数据库

- ✓ 迁移已应用
- ✓ 现有数据已修复

## 后续维护

### 新环境部署

在新环境部署时，只需执行：

```bash
conda activate mai_notebook
python manage.py migrate
python manage.py init
```

ai_reviewer 账号会自动创建并正确配置。

### 现有环境更新

在现有环境更新时，需要执行：

```bash
conda activate mai_notebook
python manage.py migrate
python fix_ai_reviewer_account.py
```

### 添加新的系统账号

如需添加新的系统账号，参考 ai_reviewer 的配置：

1. 在 `init_users.json` 中添加账号配置
2. 设置 `user_type: 2`
3. 设置 `is_active: false`
4. 设置 `password: "!"`
5. 运行 `python manage.py init`

## 相关文件清单

### 后端文件

- `backend/mainotebook/system/models.py` - Users 模型
- `backend/mainotebook/system/migrations/0006_add_system_account_type.py` - 数据库迁移
- `backend/mainotebook/system/fixtures/init_users.json` - 初始化配置
- `backend/mainotebook/system/views/user.py` - 用户 API
- `backend/mainotebook/content/services/user_moderation_service.py` - 处罚服务
- `backend/test_system_account_protection.py` - 测试脚本
- `backend/fix_ai_reviewer_account.py` - 数据修复脚本

### 前端文件

- `admin_web/src/views/content/moderation/punish/index.vue` - 处罚操作页面

### 文档文件

- `backend/docs/archive/系统账号保护机制实现完成说明.md` - 本文档

## 完成状态

✓ 所有功能已实现并测试通过
✓ 数据库迁移已应用
✓ 现有数据已修复
✓ 测试脚本验证通过

---

**实现人员**: Kiro AI Assistant  
**完成日期**: 2026-03-07  
**文档版本**: 1.0
