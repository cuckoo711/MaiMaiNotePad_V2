# 人设卡版本号筛选功能说明

## 完成时间

2026-03-03

## 功能概述

为人设卡广场添加版本号展示和筛选功能，支持精确匹配和泛筛选（通配符）。

## 功能特性

### 1. 版本号展示

- ✅ 卡片列表展示版本号徽章
- ✅ 详情弹窗显示版本号信息
- ✅ 渐变色徽章设计，视觉效果突出

### 2. 版本号筛选

支持多种筛选格式：

- **精确匹配**：`1.2.3` - 精确匹配版本号
- **主版本泛筛选**：`1` 或 `1.*` - 匹配所有 1.x.x 版本
- **主次版本泛筛选**：`1.2` 或 `1.2.*` - 匹配所有 1.2.x 版本

### 3. 用户界面

- 左侧边栏版本筛选区域
- 输入框支持回车键快速筛选
- 清除按钮快速重置筛选
- 提示文本说明支持的格式

## 后端实现

### 1. 过滤器增强

文件：`backend/mainotebook/content/filters.py`

在 `PersonaCardFilter` 中添加了 `version` 字段和 `filter_version` 方法：

```python
# 版本号筛选（支持泛筛选）
version = filters.CharFilter(
    method='filter_version',
    label='版本号'
)

def filter_version(self, queryset, name, value):
    """版本号筛选过滤方法
    
    支持以下格式：
    - 精确匹配：1.2.3
    - 主版本泛筛选：1.* 或 1（匹配 1.x.x）
    - 主次版本泛筛选：1.2.* 或 1.2（匹配 1.2.x）
    
    Args:
        queryset: 查询集
        name: 字段名
        value: 版本号字符串
        
    Returns:
        QuerySet: 过滤后的查询集
    """
    if not value:
        return queryset
    
    # 移除空格
    value = value.strip()
    
    # 处理通配符 *
    if value.endswith('.*'):
        # 移除 .* 后缀
        prefix = value[:-2]
        return queryset.filter(version__startswith=prefix + '.')
    elif value.endswith('*'):
        # 移除 * 后缀
        prefix = value[:-1]
        return queryset.filter(version__startswith=prefix)
    elif '.' not in value:
        # 只有主版本号，如 "1"，匹配 1.x.x
        return queryset.filter(version__startswith=value + '.')
    elif value.count('.') == 1:
        # 主次版本号，如 "1.2"，匹配 1.2.x
        return queryset.filter(version__startswith=value + '.')
    else:
        # 完整版本号，精确匹配
        return queryset.filter(version=value)
```

### 2. API 参数

版本号筛选通过 `version` 查询参数传递：

```
GET /api/content/persona/?version=1.2.3
GET /api/content/persona/?version=1.*
GET /api/content/persona/?version=1.2
```

## 前端实现

### 1. UI 组件

文件：`admin_web/src/views/content/persona/index.vue`

#### 左侧边栏筛选区域

```vue
<div class="sidebar-section">
  <div class="section-title">版本筛选</div>
  <div class="version-filter">
    <el-input
      v-model="versionFilter"
      placeholder="如: 1.2.3 或 1.* 或 1.2"
      clearable
      @clear="handleVersionFilter"
      @keyup.enter="handleVersionFilter"
    >
      <template #prefix>
        <i class="fa fa-code-fork"></i>
      </template>
    </el-input>
    <el-button 
      type="primary" 
      size="small" 
      style="margin-top: 8px; width: 100%"
      @click="handleVersionFilter"
    >
      应用筛选
    </el-button>
    <div class="version-tips">
      <small>
        支持格式：<br>
        • 精确版本：1.2.3<br>
        • 主版本：1 或 1.*<br>
        • 主次版本：1.2 或 1.2.*
      </small>
    </div>
  </div>
</div>
```

#### 卡片版本号展示

```vue
<div class="card-version" v-if="item.version">
  <span class="version-badge">
    <i class="fa fa-code-fork"></i> v{{ item.version }}
  </span>
</div>
```

#### 详情弹窗版本号展示

```vue
<div class="info-row">
  <span class="info-label">版本号:</span>
  <span class="info-value">
    <span class="version-badge">v{{ currentDetail.version || '1.0' }}</span>
  </span>
</div>
```

### 2. 状态管理

```typescript
// 版本筛选状态
const versionFilter = ref('');

// 版本号筛选处理
const handleVersionFilter = () => {
  currentPage.value = 1;
  loadPersonaList();
};

// 加载列表时传递版本参数
const loadPersonaList = async () => {
  const params: any = {
    page: currentPage.value,
    page_size: pageSize.value,
  };

  // 版本号筛选
  if (versionFilter.value) {
    params.version = versionFilter.value.trim();
  }

  const res = await api.getPersonaList(params);
  // ...
};
```

### 3. 样式设计

#### 版本号徽章样式

```scss
.version-badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 3px 10px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 12px;
  font-size: 11px;
  font-weight: 500;
}
```

#### 筛选提示样式

```scss
.version-filter {
  .version-tips {
    margin-top: 8px;
    padding: 8px;
    background: #f9f9f9;
    border-radius: 4px;
    
    small {
      color: #666;
      line-height: 1.6;
    }
  }
}
```

## 使用示例

### 1. 精确匹配

筛选版本号为 `1.2.3` 的人设卡：

```
输入：1.2.3
结果：只返回 version = "1.2.3" 的人设卡
```

### 2. 主版本泛筛选

筛选所有 1.x.x 版本的人设卡：

```
输入：1 或 1.*
结果：返回 version 以 "1." 开头的所有人设卡
      如：1.0.0, 1.2.3, 1.5.8 等
```

### 3. 主次版本泛筛选

筛选所有 1.2.x 版本的人设卡：

```
输入：1.2 或 1.2.*
结果：返回 version 以 "1.2." 开头的所有人设卡
      如：1.2.0, 1.2.3, 1.2.10 等
```

## 技术细节

### 1. 版本号格式

- 标准格式：`X.Y.Z`（主版本.次版本.修订版本）
- 存储字段：`PersonaCard.version`（CharField, max_length=50）
- 默认值：`"1.0"`

### 2. 筛选逻辑

使用 Django ORM 的 `startswith` 查询：

```python
# 主版本筛选：1 -> 1.
queryset.filter(version__startswith='1.')

# 主次版本筛选：1.2 -> 1.2.
queryset.filter(version__startswith='1.2.')

# 精确匹配：1.2.3
queryset.filter(version='1.2.3')
```

### 3. 性能优化

- 版本号字段已建立索引（在 `PersonaCard` 模型的 Meta 中）
- 使用 `startswith` 查询可以利用索引
- 前端输入框支持防抖，避免频繁请求

## 注意事项

1. **版本号格式**：建议遵循语义化版本规范（Semantic Versioning）
2. **输入验证**：前端不强制验证格式，后端会处理各种输入
3. **空值处理**：如果人设卡没有版本号，默认显示 `v1.0`
4. **大小写**：版本号筛选不区分大小写（虽然通常版本号只包含数字和点）

## 相关文档

- [人设卡广场功能配置说明](./人设卡广场功能配置说明.md)
- [人设卡广场功能完成总结](./人设卡广场功能完成总结.md)
- [内容管理功能文档](../功能文档/内容管理.md)
