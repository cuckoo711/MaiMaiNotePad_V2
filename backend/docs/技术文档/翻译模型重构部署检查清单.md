# 翻译模型重构部署检查清单

## 概述

本检查清单用于确保翻译模型重构功能的顺利部署。请按照顺序逐项检查，确保每个步骤都已完成并验证通过。

**部署负责人**: _______________  
**部署日期**: _______________  
**环境**: [ ] 测试环境 [ ] 预发布环境 [ ] 生产环境

---

## 一、部署前检查

### 1.1 环境准备

- [ ] **Conda 环境已激活**
  ```bash
  conda activate mai_notebook
  python --version  # 确认 Python 3.8+
  ```

- [ ] **数据库连接正常**
  ```bash
  python manage.py dbshell
  # 能够成功连接数据库
  ```

- [ ] **Redis 连接正常**
  ```bash
  redis-cli ping
  # 返回 PONG
  ```

- [ ] **依赖包已安装**
  ```bash
  pip list | grep -E "django|djangorestframework|hypothesis"
  # 确认所有依赖已安装
  ```

### 1.2 代码准备

- [ ] **代码已更新到最新版本**
  ```bash
  git pull origin main
  git log -1  # 确认最新提交
  ```

- [ ] **所有测试通过**
  ```bash
  cd backend
  pytest tests/test_translation_*.py
  # 所有测试通过，无失败
  ```

- [ ] **代码审查已完成**
  - 模型定义正确
  - 序列化器验证完整
  - API 权限配置正确
  - 前端代码更新正确

### 1.3 数据备份

- [ ] **数据库已备份**
  ```bash
  # PostgreSQL
  pg_dump -U postgres -d mainotebook > backup_$(date +%Y%m%d_%H%M%S).sql
  
  # MySQL
  mysqldump -u root -p mainotebook > backup_$(date +%Y%m%d_%H%M%S).sql
  ```
  
  备份文件路径: _______________

- [ ] **备份文件已验证**
  ```bash
  # 检查备份文件大小
  ls -lh backup_*.sql
  # 确认文件大小合理（不为 0）
  ```

- [ ] **备份文件已存档**
  - 备份文件已复制到安全位置
  - 备份文件命名包含日期时间

### 1.4 部署计划确认

- [ ] **部署时间窗口已确认**
  - 开始时间: _______________
  - 预计结束时间: _______________
  - 是否需要停机: [ ] 是 [ ] 否

- [ ] **回滚方案已准备**
  - 回滚步骤已文档化
  - 回滚负责人已指定
  - 回滚时间窗口已确认

- [ ] **相关人员已通知**
  - [ ] 开发团队
  - [ ] 运维团队
  - [ ] 测试团队
  - [ ] 产品团队

---

## 二、部署执行

### 2.1 数据库迁移

- [ ] **生成迁移文件**
  ```bash
  cd backend
  python manage.py makemigrations
  ```
  
  迁移文件: _______________

- [ ] **检查迁移计划**
  ```bash
  python manage.py showmigrations
  # 确认待执行的迁移
  ```

- [ ] **执行数据库迁移**
  ```bash
  python manage.py migrate
  ```
  
  执行时间: _______________  
  执行结果: [ ] 成功 [ ] 失败

- [ ] **验证表结构**
  ```bash
  python manage.py shell
  >>> from mainotebook.system.models import Translation
  >>> Translation.objects.count()
  0
  >>> exit()
  ```

### 2.2 数据迁移

- [ ] **模拟运行数据迁移**
  ```bash
  python manage.py migrate_translation --dry-run
  ```
  
  预计迁移记录数: _______________  
  是否有错误: [ ] 是 [ ] 否

- [ ] **执行数据迁移**
  ```bash
  python manage.py migrate_translation
  ```
  
  执行时间: _______________  
  成功记录数: _______________  
  失败记录数: _______________

- [ ] **验证迁移结果**
  ```bash
  python manage.py shell
  >>> from mainotebook.system.models import Translation
  >>> Translation.objects.count()
  # 应该等于预期的记录数
  >>> Translation.objects.filter(translation_type='toml_visualizer_blocks').count()
  # 检查块翻译数量
  >>> Translation.objects.filter(translation_type='toml_visualizer_tokens').count()
  # 检查 Token 翻译数量
  >>> exit()
  ```

- [ ] **数据完整性检查**
  ```bash
  python manage.py shell
  >>> from mainotebook.system.models import Translation
  >>> # 检查字段映射
  >>> trans = Translation.objects.first()
  >>> print(f"{trans.source_text} -> {trans.translated_text}")
  >>> # 检查是否有空值
  >>> Translation.objects.filter(source_text='').count()
  0
  >>> Translation.objects.filter(translated_text='').count()
  0
  >>> exit()
  ```

### 2.3 API 配置

- [ ] **检查 API 白名单**
  ```bash
  python manage.py shell
  >>> from mainotebook.system.models import ApiWhiteList
  >>> ApiWhiteList.objects.filter(url__contains='translations').values('url', 'method')
  # 应该包含两条记录
  >>> exit()
  ```

- [ ] **添加白名单（如需要）**
  ```bash
  python manage.py shell
  >>> from mainotebook.system.models import ApiWhiteList
  >>> ApiWhiteList.objects.get_or_create(
  ...     url='/api/translations/',
  ...     method=0,
  ...     defaults={'enable_datasource': False}
  ... )
  >>> ApiWhiteList.objects.get_or_create(
  ...     url='/api/translations/get_by_type/',
  ...     method=0,
  ...     defaults={'enable_datasource': False}
  ... )
  >>> exit()
  ```

### 2.4 后端服务重启

- [ ] **停止当前服务**
  ```bash
  # 记录当前进程 ID
  ps aux | grep "python main.py"
  # 停止服务（Ctrl+C 或 kill）
  ```

- [ ] **启动新服务**
  ```bash
  python main.py
  # 或使用启动脚本
  bash start_backend.sh
  ```
  
  启动时间: _______________  
  启动结果: [ ] 成功 [ ] 失败

- [ ] **检查服务状态**
  ```bash
  # 检查进程
  ps aux | grep "python main.py"
  
  # 检查端口
  lsof -i :8000
  
  # 检查日志
  tail -f logs/django.log
  ```

### 2.5 前端部署

- [ ] **安装前端依赖**
  ```bash
  cd admin_web
  npm install
  ```

- [ ] **构建前端（生产环境）**
  ```bash
  npm run build
  ```
  
  构建时间: _______________  
  构建结果: [ ] 成功 [ ] 失败

- [ ] **部署静态文件（生产环境）**
  ```bash
  # 复制构建产物到服务器
  rsync -avz dist/ user@server:/path/to/static/
  ```

---

## 三、部署后验证

### 3.1 后端 API 验证

- [ ] **测试翻译列表接口**
  ```bash
  curl http://localhost:8000/api/translations/
  ```
  
  响应状态码: _______________  
  响应数据正确: [ ] 是 [ ] 否

- [ ] **测试按类型查询接口（块翻译）**
  ```bash
  curl "http://localhost:8000/api/translations/get_by_type/?translation_type=toml_visualizer_blocks"
  ```
  
  响应状态码: _______________  
  返回记录数: _______________

- [ ] **测试按类型查询接口（Token 翻译）**
  ```bash
  curl "http://localhost:8000/api/translations/get_by_type/?translation_type=toml_visualizer_tokens"
  ```
  
  响应状态码: _______________  
  返回记录数: _______________

- [ ] **测试单个翻译查询**
  ```bash
  # 获取一个翻译 ID
  curl "http://localhost:8000/api/translations/" | jq '.results[0].id'
  
  # 查询该翻译
  curl "http://localhost:8000/api/translations/<id>/"
  ```
  
  响应状态码: _______________  
  响应数据正确: [ ] 是 [ ] 否

- [ ] **验证响应格式**
  - [ ] 包含 `source_text` 字段
  - [ ] 包含 `translated_text` 字段
  - [ ] 包含 `translation_type` 字段
  - [ ] 包含 `source_language` 字段
  - [ ] 包含 `target_language` 字段
  - [ ] 包含 `sort` 字段
  - [ ] 包含 `status` 字段

### 3.2 权限验证

- [ ] **未认证用户可以查询**
  ```bash
  # 不带 Authorization 头
  curl http://localhost:8000/api/translations/
  ```
  
  响应状态码: _______________（应为 200）

- [ ] **未认证用户无法创建**
  ```bash
  curl -X POST http://localhost:8000/api/translations/ \
    -H "Content-Type: application/json" \
    -d '{"source_text":"test","translated_text":"测试","translation_type":"test"}'
  ```
  
  响应状态码: _______________（应为 401 或 403）

- [ ] **管理员可以创建**
  ```bash
  curl -X POST http://localhost:8000/api/translations/ \
    -H "Authorization: JWT <admin_token>" \
    -H "Content-Type: application/json" \
    -d '{"source_text":"test","translated_text":"测试","translation_type":"test","source_language":"en","target_language":"zh"}'
  ```
  
  响应状态码: _______________（应为 201）

### 3.3 前端功能验证

- [ ] **前端页面可访问**
  - 访问管理后台首页
  - 页面正常加载，无白屏

- [ ] **TOML 可视化解析器正常**
  - [ ] 进入人设卡管理页面
  - [ ] 点击查看人设卡配置文件
  - [ ] TOML 可视化解析器正常显示

- [ ] **翻译功能正常**
  - [ ] 块名显示中文翻译（如：character -> 角色信息）
  - [ ] Token 名显示中文翻译（如：char_name -> 角色名称）
  - [ ] 找不到翻译时显示原始英文

- [ ] **浏览器控制台无错误**
  - 打开浏览器开发者工具（F12）
  - 检查 Console 标签，无 JavaScript 错误
  - 检查 Network 标签，API 请求成功

### 3.4 性能验证

- [ ] **API 响应时间**
  ```bash
  # 测试响应时间
  time curl "http://localhost:8000/api/translations/get_by_type/?translation_type=toml_visualizer_blocks"
  ```
  
  响应时间: _______________ ms（应 < 200ms）

- [ ] **数据库查询性能**
  ```sql
  EXPLAIN ANALYZE SELECT * FROM dvadmin_system_translation 
  WHERE translation_type = 'toml_visualizer_blocks' 
  ORDER BY sort;
  ```
  
  查询时间: _______________ ms（应 < 50ms）

- [ ] **前端加载性能**
  - 打开浏览器开发者工具 Network 标签
  - 刷新页面
  - 检查翻译数据加载时间
  
  加载时间: _______________ ms（应 < 500ms）

### 3.5 数据完整性验证

- [ ] **翻译数据完整**
  ```bash
  python manage.py shell
  >>> from mainotebook.system.models import Translation, Dictionary
  >>> 
  >>> # 检查迁移的记录数
  >>> trans_count = Translation.objects.count()
  >>> print(f"Translation 记录数: {trans_count}")
  >>> 
  >>> # 检查原字典记录数（迁移前）
  >>> dict_count = Dictionary.objects.filter(
  ...     parent__value__in=['toml_visualizer_blocks', 'toml_visualizer_tokens']
  ... ).count()
  >>> print(f"Dictionary 记录数: {dict_count}")
  >>> 
  >>> # 应该相等
  >>> assert trans_count == dict_count
  >>> exit()
  ```

- [ ] **字段映射正确**
  ```bash
  python manage.py shell
  >>> from mainotebook.system.models import Translation, Dictionary
  >>> 
  >>> # 随机抽查几条记录
  >>> trans = Translation.objects.filter(translation_type='toml_visualizer_tokens').first()
  >>> dict_record = Dictionary.objects.filter(value=trans.source_text).first()
  >>> 
  >>> # 验证映射
  >>> assert trans.translated_text == dict_record.label
  >>> assert trans.source_text == dict_record.value
  >>> exit()
  ```

- [ ] **无重复记录**
  ```bash
  python manage.py shell
  >>> from mainotebook.system.models import Translation
  >>> from django.db.models import Count
  >>> 
  >>> duplicates = Translation.objects.values('translation_type', 'source_text').annotate(
  ...     count=Count('id')
  ... ).filter(count__gt=1)
  >>> 
  >>> print(f"重复记录数: {duplicates.count()}")
  >>> assert duplicates.count() == 0
  >>> exit()
  ```

### 3.6 日志检查

- [ ] **后端日志无错误**
  ```bash
  tail -100 logs/django.log | grep -i error
  # 应该没有相关错误
  ```

- [ ] **数据库日志无错误**
  ```bash
  # PostgreSQL
  tail -100 /var/log/postgresql/postgresql-*.log | grep -i error
  
  # MySQL
  tail -100 /var/log/mysql/error.log
  ```

- [ ] **Nginx 日志正常（如使用）**
  ```bash
  tail -100 /var/log/nginx/access.log | grep translations
  tail -100 /var/log/nginx/error.log | grep translations
  ```

---

## 四、清理旧数据（可选）

**警告**: 只有在确认新系统运行正常后才执行此步骤！

### 4.1 备份旧数据

- [ ] **执行备份**
  ```bash
  python manage.py cleanup_translation_dict --backup
  ```
  
  备份文件: _______________  
  备份记录数: _______________

- [ ] **验证备份文件**
  ```bash
  # 检查备份文件
  cat translation_backup_*.json | jq length
  # 确认记录数正确
  ```

### 4.2 清理字典数据

- [ ] **执行清理**
  ```bash
  python manage.py cleanup_translation_dict --backup --force
  ```
  
  删除记录数: _______________  
  执行结果: [ ] 成功 [ ] 失败

- [ ] **验证清理结果**
  ```bash
  python manage.py shell
  >>> from mainotebook.system.models import Dictionary
  >>> Dictionary.objects.filter(value='toml_visualizer_blocks').count()
  0
  >>> Dictionary.objects.filter(value='toml_visualizer_tokens').count()
  0
  >>> exit()
  ```

- [ ] **功能仍然正常**
  - 重新测试前端 TOML 可视化解析器
  - 确认翻译功能正常工作

---

## 五、监控设置

### 5.1 性能监控

- [ ] **配置 API 性能监控**
  - 监控 `/api/translations/` 响应时间
  - 监控 `/api/translations/get_by_type/` 响应时间
  - 设置告警阈值（如 > 500ms）

- [ ] **配置数据库性能监控**
  - 监控 Translation 表查询时间
  - 监控慢查询日志
  - 检查索引使用情况

- [ ] **配置前端性能监控**
  - 监控翻译数据加载时间
  - 监控页面渲染时间
  - 收集用户体验指标

### 5.2 错误监控

- [ ] **配置错误日志监控**
  - 监控后端错误日志
  - 监控前端 JavaScript 错误
  - 设置告警通知

- [ ] **配置 API 错误率监控**
  - 监控 4xx 错误率
  - 监控 5xx 错误率
  - 设置告警阈值（如 > 1%）

### 5.3 数据监控

- [ ] **配置数据完整性监控**
  - 定期检查翻译记录数
  - 检查是否有重复记录
  - 检查是否有空值记录

---

## 六、文档更新

- [ ] **更新部署文档**
  - 记录实际部署步骤
  - 记录遇到的问题和解决方案
  - 更新版本号和日期

- [ ] **更新 API 文档**
  - 添加新的翻译 API 接口文档
  - 更新 Swagger/ReDoc 文档

- [ ] **更新 README**
  - 更新功能列表
  - 更新依赖说明
  - 更新快速开始指南

- [ ] **更新文档索引**
  ```bash
  # 在 backend/docs/README.md 中添加新文档链接
  ```

---

## 七、团队通知

- [ ] **通知开发团队**
  - 部署已完成
  - 新功能已上线
  - 相关文档链接

- [ ] **通知测试团队**
  - 请求进行回归测试
  - 提供测试要点
  - 提供测试环境信息

- [ ] **通知产品团队**
  - 功能已上线
  - 提供功能说明
  - 收集反馈

- [ ] **通知运维团队**
  - 需要关注的监控指标
  - 可能的风险点
  - 回滚方案

---

## 八、回滚准备（如需要）

### 8.1 回滚触发条件

以下情况需要考虑回滚：

- [ ] API 错误率 > 5%
- [ ] 数据库查询时间 > 1s
- [ ] 前端功能无法正常使用
- [ ] 发现严重的数据错误
- [ ] 用户投诉量激增

### 8.2 回滚步骤

- [ ] **停止当前服务**
  ```bash
  # 停止后端服务
  ps aux | grep "python main.py"
  kill <pid>
  ```

- [ ] **回滚代码**
  ```bash
  git revert <commit-hash>
  git push
  # 或
  git checkout <previous-commit>
  ```

- [ ] **回滚数据库迁移**
  ```bash
  python manage.py migrate system <previous_migration_number>
  ```

- [ ] **恢复数据库备份（如需要）**
  ```bash
  # PostgreSQL
  psql -U postgres -d mainotebook < backup_YYYYMMDD_HHMMSS.sql
  
  # MySQL
  mysql -u root -p mainotebook < backup_YYYYMMDD_HHMMSS.sql
  ```

- [ ] **重启服务**
  ```bash
  bash start_backend.sh
  ```

- [ ] **验证回滚结果**
  - 检查服务是否正常启动
  - 检查功能是否恢复正常
  - 检查数据是否完整

### 8.3 回滚后处理

- [ ] **通知相关团队**
  - 说明回滚原因
  - 说明当前状态
  - 说明后续计划

- [ ] **分析回滚原因**
  - 记录问题详情
  - 分析根本原因
  - 制定改进方案

- [ ] **准备重新部署**
  - 修复发现的问题
  - 更新测试用例
  - 制定新的部署计划

---

## 九、部署总结

### 9.1 部署结果

- [ ] **部署成功**
  - 所有检查项通过
  - 功能正常运行
  - 性能符合预期

- [ ] **部署失败**
  - 失败原因: _______________
  - 已执行回滚: [ ] 是 [ ] 否
  - 后续计划: _______________

### 9.2 遇到的问题

| 问题描述 | 解决方案 | 负责人 | 状态 |
|---------|---------|--------|------|
|         |         |        |      |
|         |         |        |      |
|         |         |        |      |

### 9.3 经验教训

**做得好的地方**:
- 
- 
- 

**需要改进的地方**:
- 
- 
- 

### 9.4 后续行动

- [ ] **持续监控**
  - 监控周期: _______________
  - 监控负责人: _______________

- [ ] **性能优化**
  - 优化计划: _______________
  - 预计完成时间: _______________

- [ ] **文档完善**
  - 需要补充的文档: _______________
  - 负责人: _______________

---

## 签字确认

**部署负责人**: _______________  
**签字**: _______________  
**日期**: _______________

**技术负责人**: _______________  
**签字**: _______________  
**日期**: _______________

**运维负责人**: _______________  
**签字**: _______________  
**日期**: _______________

---

## 附录

### A. 常用命令

```bash
# 激活环境
conda activate mai_notebook

# 数据库迁移
python manage.py makemigrations
python manage.py migrate

# 数据迁移
python manage.py migrate_translation --dry-run
python manage.py migrate_translation

# 清理旧数据
python manage.py cleanup_translation_dict --backup

# 启动服务
python main.py
bash start_backend.sh

# 查看日志
tail -f logs/django.log

# 测试 API
curl http://localhost:8000/api/translations/
curl "http://localhost:8000/api/translations/get_by_type/?translation_type=toml_visualizer_blocks"
```

### B. 联系方式

| 角色 | 姓名 | 联系方式 |
|------|------|---------|
| 开发负责人 |  |  |
| 运维负责人 |  |  |
| 测试负责人 |  |  |
| 产品负责人 |  |  |

### C. 相关文档

- [翻译模型重构部署指南](./翻译模型重构部署指南.md)
- [需求文档](.kiro/specs/translation-model-refactor/requirements.md)
- [设计文档](.kiro/specs/translation-model-refactor/design.md)
- [任务列表](.kiro/specs/translation-model-refactor/tasks.md)

---

**文档版本**: v1.0.0  
**最后更新**: 2024-01-01  
**维护人**: 开发团队
